# Gradle Properties
org.gradle.jvmargs = -Xmx1G
org.gradle.parallel = true

# Mod Properties
version = 0.0.1
maven_group = dev.proxyfox.mod
archives_base_name = maprender

# Dependencies are managed at gradle/libs.versions.toml
